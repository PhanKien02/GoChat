info:
  name: get user
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/users/:id"
  params:
    - name: id
      value: 6a1e97e23c49d3e381557f4d
      type: path
  body:
    type: json
    data: |-
      {
        "username":"abcd",
        "password":"ádjfádfádfád",
        "email":"demo@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
