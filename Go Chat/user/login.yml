info:
  name: login
  type: http
  seq: 3

http:
  method: post
  url: "{{BASE_URL}}/auth/loginn"
  body:
    type: json
    data: |-
      {
        "login":"demo@gmail.com",
        "password":"ádjfádfádfád"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
